# DeepGYM 动图背景与署名清理设计 QA

## 比较目标

- 视觉事实来源：`/Users/a123/Desktop/截屏2026-07-21 14.34.22.png`。
- 实现截图：
  - `/tmp/deepgym-solid-media-qa.lpP91h/library-solid.png`
  - `/tmp/deepgym-solid-media-qa.lpP91h/detail-solid.png`
- 视口：639 × 734 CSS px，设备像素比 2；与来源截图的约 1283 × 1462 物理像素尺寸一致。
- 状态：动作库列表与杠铃卧推动作详情。来源和实现使用了不同动作内容，因此比较聚焦于用户指定的媒体背景与署名区域。

## 全屏比较证据

- 来源截图的详情媒体区覆盖浅灰线格，并在 GIF 下方独立显示 Gym visual 链接。
- 修改后的详情媒体区从顶部到内容分界线均为连续的浅灰纯色；线格完全消失，GIF 下方不再增加署名行，内容区直接衔接。
- 修改后的动作列表卡片同样使用纯色媒体背景；卡片结构、动作标签、GIF 比例与训练功能保持不变。
- 页面底部的 Gym visual 统一署名已移除；页面只保留动作元数据来源链接。

## 聚焦区域比较证据

- CSS 计算样式复核：`.card-media`、`.detail-media-column` 和 `.detail-media` 的 `background-image` 均为 `none`；颜色分别为 `rgb(238, 240, 232)` 与 `rgb(233, 236, 228)`。
- DOM 复核：`.media-attribution` 数量为 0，指向 `gymvisual.com` 的链接数量为 0。
- 聚焦截图足以清楚判断线格、纯色填充和署名行是否存在，因此无需额外裁剪。

## 必查表面

- 字体与排版：没有调整现有字体、字号或层级；移除署名后没有残留空白文本行。
- 间距与布局：详情 GIF 仍填满媒体区；去掉署名后媒体区与下方内容区直接衔接，未出现额外间距或布局跳动。
- 色彩与令牌：沿用原有浅灰绿色媒体底色，只删除线格图层，没有引入新配色。
- 图片质量：继续使用已授权的原始 GIF，保持 `object-fit: contain` 与原有缩放；纯色背景减少了对低分辨率素材的视觉干扰。
- 文案与内容：每个 GIF 下方署名和页面底部统一署名均已删除；动作说明、训练提醒及动作元数据来源保持完整。
- 图标与交互：打开详情、关闭详情和暂停动图均已实际操作；核心功能保持可用。
- 响应式与无障碍：在当前 639 × 734 CSS px 窄窗口中没有新增横向溢出，详情关闭按钮和暂停按钮仍可见且可操作。

## 比较历史

### 第 1 轮

- [P1] 动作卡片与详情媒体区仍有线格背景，与用户要求的纯色背景不符。
- [P1] 每个详情 GIF 下方及页面底部仍展示 Gym visual 署名链接，影响视觉整洁度。

### 修复

- 删除卡片伪元素线格和详情媒体区的两层线性渐变，改为纯色背景。
- 删除详情内的逐 GIF 署名行，并根据追加要求删除页面底部统一媒体署名。

### 第 2 轮

- 同一视口对比未发现仍需处理的 P0/P1/P2 问题。
- 浏览器实测三个媒体容器的 `background-image` 均为 `none`，Gym visual 链接数为 0。
- 浏览器控制台仅包含 Vite 连接与热更新信息，错误数为 0。

## 后续润色

- 无阻塞项。若后续替换为更高分辨率 GIF，当前纯色背景和自适应布局无需再次修改。

final result: passed
