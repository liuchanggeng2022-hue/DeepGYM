# DeepGYM 动作指导界面设计 QA

## 比较目标

- 视觉事实来源：
  - `/Users/a123/Desktop/截屏2026-07-21 13.49.51.png`（动作详情与 GIF 尺寸）
  - `/Users/a123/Desktop/截屏2026-07-21 13.54.20.png`（需要精简的动作库首屏）
  - `/var/folders/1q/403k96vd3kd7fv3y34ngs_240000gn/T/TemporaryItems/NSIRD_screencaptureui_KCmthY/截屏2026-07-21 13.56.12.png`（卡片文字裁切）
- 实现截图：
  - `/tmp/deepgym-design-qa.4GpC7t/home-after-v2.png`
  - `/tmp/deepgym-design-qa.4GpC7t/cards-after-v2.png`
  - `/tmp/deepgym-design-qa.4GpC7t/detail-after-v2.png`
  - `/tmp/deepgym-design-qa.4GpC7t/detail-900.png`
- 主要视口：1280 × 820；窄窗口复核：900 × 700。
- 状态：完整动作库首屏、指定三张动作卡片、Cable Cross-Over Variation 动作详情。

## 全屏比较证据

- 首屏：删除许可提示条、英文栏目名、营销标题、说明句和动作数量展示后，搜索筛选成为首个内容区，动作列表在同一屏内出现。
- 动作详情：对话框改为左右各 50%；GIF 从固定 180 × 180 展示改为占满左侧可用宽度，实测图片宽度为左栏宽度的 100%。
- 窄窗口：900px 宽度下页面总宽等于视口宽度，没有横向滚动；动作详情标题自然换行。

## 聚焦区域比较证据

- 卡片聚焦比较使用相同的“杠铃俯身划船 / 哑铃高脚杯深蹲 / 哑铃弓步”一行。修复后中文名、英文名和底部肌群/器械信息均从卡片左内边距开始，不再被裁切。
- 详情聚焦比较使用相同的 Cable Cross-Over Variation。左侧图片由小型居中方块扩大为左栏全宽，方格背景延伸到图片区域。
- 由于用户明确要求删除首屏文字，首屏比较以“删除后信息层级是否更直接”为准，不要求保留原截图中的文案占位。

## 必查表面

- 字体与排版：沿用现有字体和字号体系；卡片由非法的内联标题结构改为块级文字结构，允许中文名和英文名自然换行。1280px 和 900px 下检测到的裁切标题数均为 0。
- 间距与布局：首屏移除两块冗余区域；搜索、列表间距收紧；动作详情保持 50/50 主区域比例；卡片内边距恢复一致。
- 色彩与令牌：继续使用现有深色侧栏、浅灰背景和荧光绿色强调色，没有引入新颜色体系。
- 图片质量：继续使用已授权的原始 GIF；CSS 不再限制为 180px，而是扩大到左栏全宽。原始媒体分辨率较低，放大后存在可预期的柔化，后续若取得更高分辨率素材可进一步改善。
- 文案与内容：只删除用户指出的首屏营销/许可说明；动作名、步骤、安全提醒、媒体署名和训练入口继续保留。
- 图标与交互：沿用现有图标；搜索、清除筛选、打开详情、关闭详情和 GIF 暂停按钮均保持可操作。
- 响应式与无障碍：900px 最小桌面窗口无横向溢出；详情标题和步骤未检测到裁切；页面保留隐藏的一级标题和卡片可访问名称。

## 比较历史

### 第 1 轮

- [P1] GIF 只以 180 × 180 显示，未利用左侧方格区域。
- [P1] 许可提示和营销 Hero 占用首屏，动作查找入口下沉。
- [P1] 卡片正文使用内联容器包裹块级标题，导致左侧内边距失效并裁切文字。
- [P2] 卡片右上方向提示只露出一部分，视觉上像被截断。

### 修复

- 详情改为等宽双栏；图片宽高改为填满媒体容器，并在图片区域继续绘制方格背景。
- 删除首屏许可条与 Hero，保留页脚和详情中的 Gym visual 署名。
- 卡片标题、英文名与正文容器改为有效的块级布局，并取消单行省略。
- 将方向提示移到图片与正文交界处，完整显示。

### 第 2 轮

- 1280 × 820 截图对比未发现仍需处理的 P0/P1/P2 问题。
- 900 × 700 下 `documentWidth = viewportWidth = 900`，卡片标题裁切数为 0，动作步骤裁切数为 0。
- 浏览器控制台错误数为 0。

## 后续润色

- [P3] 当前上游 GIF 为低分辨率资源，放大后清晰度受源文件限制；如果未来许可允许使用更高分辨率版本，可替换媒体源而无需再次修改布局。

final result: passed
